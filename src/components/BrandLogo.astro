<div class="atelier-logo atelier-logo-container" aria-label="NORRIS'S ATELIER">
  <svg
    class="atelier-logo__mark"
    viewBox="0 0 92 66"
    role="img"
    aria-hidden="true"
    focusable="false"
  >
    <g class="atelier-logo__arc-wrap">
      <path
        class="atelier-logo__arc"
        d="M20 30 A26 26 0 0 1 72 30"
        pathLength="100"
        fill="none"
        stroke="#34495E"
        stroke-width="3"
        stroke-linecap="round"
      />
    </g>
    <g class="atelier-logo__pillars">
      <rect x="20" y="34" width="14" height="22" rx="1" fill="#34495E"></rect>
      <rect x="58" y="34" width="14" height="22" rx="1" fill="#34495E"></rect>
    </g>
  </svg>
  <p class="atelier-logo__kicker atelier-logo__text">NORRIS'S</p>
  <p class="atelier-logo__title atelier-logo__text">ATELIER</p>
</div>

<style>
  .atelier-logo-container {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 0.12rem;
    width: 4.7rem;
    background: transparent;
    padding: 0.06rem 0;
    text-align: center;
    line-height: 1;
    transform-origin: center;
    transition: transform 0.3s ease-out;
    will-change: transform;
  }

  .atelier-logo-container:hover {
    transform: translateY(-2px) scale(1.02);
  }

  .atelier-logo__mark {
    width: 2.2rem;
    height: auto;
    display: block;
  }

  .atelier-logo__arc-wrap {
    transform-box: fill-box;
    transform-origin: 50% 46%;
    animation: atelier-arc-idle 3s cubic-bezier(0.45, 0, 0.2, 1) 1.2s infinite;
    will-change: transform;
  }

  .atelier-logo__arc {
    stroke-dasharray: 100;
    stroke-dashoffset: 100;
    transform-box: fill-box;
    transform-origin: center;
    transition: transform 0.3s ease-out;
    animation: atelier-arc-draw 1s cubic-bezier(0.4, 0, 0.2, 1) forwards;
  }

  .atelier-logo-container:hover .atelier-logo__arc {
    transform: translateY(-1px);
  }

  .atelier-logo__pillars {
    opacity: 0;
    transform: translateY(3px);
    animation: atelier-pillars-in 0.6s cubic-bezier(0.22, 0.61, 0.36, 1) 0.5s
      forwards;
  }

  .atelier-logo__text {
    opacity: 0;
    animation: atelier-text-fade 0.5s cubic-bezier(0.4, 0, 0.2, 1) 1s forwards;
  }

  .atelier-logo__kicker,
  .atelier-logo__title {
    margin: 0;
  }

  .atelier-logo__kicker {
    margin-top: 0.02rem;
    font-family: "Inter", "PingFang SC", "Noto Sans SC", sans-serif;
    font-size: 8px;
    font-weight: 500;
    letter-spacing: 0.3em;
    color: #7f8c8d;
    text-indent: 0.3em;
  }

  .atelier-logo__title {
    margin-top: 0.01rem;
    display: block;
    width: 100%;
    font-family: "Lora", "Source Han Serif SC", "Noto Serif SC", serif;
    font-size: 14px;
    font-weight: 500;
    letter-spacing: 0.1em;
    color: #2c3e50;
    animation:
      atelier-text-fade 0.5s cubic-bezier(0.4, 0, 0.2, 1) 1s forwards,
      atelier-atelier-spacing 0.5s cubic-bezier(0.25, 0.46, 0.45, 0.94) 1s
        forwards;
    transition: color 0.3s ease-out;
  }

  .atelier-logo-container:hover .atelier-logo__title {
    color: #46535f;
  }
  @media (max-width: 640px) {
    .atelier-logo__mark {
      width: 2rem;
    }

    .atelier-logo__title {
      font-size: 13px;
    }

    .atelier-logo__kicker {
      font-size: 7px;
    }
  }

  @keyframes atelier-arc-draw {
    from {
      stroke-dashoffset: 100;
    }
    to {
      stroke-dashoffset: 0;
    }
  }

  @keyframes atelier-pillars-in {
    from {
      opacity: 0;
      transform: translateY(3px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes atelier-text-fade {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }

  @keyframes atelier-atelier-spacing {
    from {
      letter-spacing: 0.05em;
    }
    to {
      letter-spacing: 0.1em;
    }
  }

  @keyframes atelier-arc-idle {
    0%,
    100% {
      transform: scale(0.998);
    }
    50% {
      transform: scale(1.002);
    }
  }
</style>
